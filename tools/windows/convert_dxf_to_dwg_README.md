# DXF 轉 DWG（Windows + AutoCAD）

1. 下載 `datacenter_floorplan.dxf`。
2. 使用 AutoCAD 開啟 `datacenter_floorplan.dxf`。
3. 可直接「另存新檔」為 DWG，或執行 `convert_dxf_to_dwg.scr` 自動另存。
4. 輸出檔案為 `datacenter_floorplan.dwg`。
